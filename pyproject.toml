[project]
name = "vpngate-linux"
version = "1.0.0"
description = "A safe, educational VPN Gate client for Linux"
readme = "README.md"
authors = [
    { name = "armin", email = "aboumelon1831@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "httpx[socks]>=0.28.1",
    "platformdirs>=4.11.0",
    "psutil>=7.2.2",
    "pydantic>=2.13.4",
    "textual>=8.2.8",
    "typer>=0.27.1",
]

[project.scripts]
vpngate = "vpngate_linux:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
